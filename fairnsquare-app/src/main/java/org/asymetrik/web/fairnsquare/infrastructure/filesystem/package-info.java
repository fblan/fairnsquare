/**
 * Filesystem infrastructure — file storage and tenant path resolution.
 */
@Module(name = "filesystem", exports = { "." })
package org.asymetrik.web.fairnsquare.infrastructure.filesystem;

import org.asymetrik.modular.api.Module;
